# Coding Standards

## Indentation

Use 2 spaces for indentation. No tabs.

## UI Components

Prefer shadcn components over hand-written UI elements. When a shadcn component can fulfill the need, use it instead of writing custom markup. This reduces styling inconsistencies and maintains visual unity across the app.

## Language

All user-facing text in the app UI must be written in English.
